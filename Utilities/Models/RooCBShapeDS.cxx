/***************************************************************************** 
 * Project: RooFit                                                           * 
 *                                                                           * 
 * This code was autogenerated by RooClassFactory                            * 
 *****************************************************************************/ 

 /** \class RooExtCBShape
     \ingroup Roofit
    
     P.d.f implementing the Extended Crystal Ball line shape
 **/

#include "Riostream.h"

#include "RooCBShapeDS.h"
#include "RooAbsReal.h"
#include "RooRealVar.h"
#include "RooMath.h"

ClassImp(RooExtCBShape);

////////////////////////////////////////////////////////////////////////////////
    
Double_t RooExtCBShape::ApproxErf(Double_t arg) const
{
  static const double erflim = 5.0;
  if( arg > erflim )
    return 1.0;
  if( arg < -erflim )
    return -1.0;
    
  return RooMath::erf(arg);
}
    
////////////////////////////////////////////////////////////////////////////////
    
RooExtCBShape::RooExtCBShape(const char *name, const char *title,
                             RooAbsReal& _m, RooAbsReal& _m0, RooAbsReal& _sigma,
                             RooAbsReal& _alpha, RooAbsReal& _n, 
                             RooAbsReal& _alpha2, RooAbsReal& _n2) :
RooAbsPdf(name, title),
  m("m", "Dependent", this, _m),
  m0("m0", "M0", this, _m0),
  sigma("sigma", "Sigma", this, _sigma),
  alpha("alpha", "Alpha", this, _alpha),
  n("n", "Order", this, _n),
  alpha2("alpha2", "Alpha2", this, _alpha2),
  n2("n2", "Order2", this, _n2)
{
}

RooExtCBShape::RooExtCBShape()
{
   RooRealVar mRV("mRV","x", 0.0, 1.0);
   RooRealVar m0RV("m0RV", "m0", 0.0, 1.0);
   RooRealVar sigmaRV("sigmaRV", "sigma", 0.0, 1.0);
   RooRealVar alphaRV("alphaRV", "alpha", 0.0, 1.0);
   RooRealVar nRV("nRV","n", 0.0, 1.0);
   RooRealVar alpha2RV("alpha2RV", "alpha2", 0.0, 1.0);
   RooRealVar n2RV("n2RV","n2", 0.0, 1.0);
   RooExtCBShape("RooExtCBShape", "RooExtCBShape", mRV, m0RV, sigmaRV, alphaRV, nRV, alpha2RV, n2RV);
}
    
////////////////////////////////////////////////////////////////////////////////
    
RooExtCBShape::RooExtCBShape(const RooExtCBShape& other, const char* name) :
  RooAbsPdf(other, name), m("m", this, other.m), m0("m0", this, other.m0),
  sigma("sigma", this, other.sigma), alpha("alpha", this, other.alpha),
  n("n", this, other.n), alpha2("alpha2", this, other.alpha2),
  n2("n2", this, other.n2)
{
}
    
////////////////////////////////////////////////////////////////////////////////
    
Double_t RooExtCBShape::evaluate() const {
  Double_t t = (m-m0)/sigma;
  if (alpha < 0) t = -t;
  
  Double_t absAlpha = fabs((Double_t)alpha);
  Double_t absAlpha2 = fabs((Double_t)alpha2);
  
  if (t >= -absAlpha && t < absAlpha2) {
    return exp(-0.5*t*t);
  }
  if (t < -absAlpha) {
    Double_t a = TMath::Power(n/absAlpha,n)*exp(-0.5*absAlpha*absAlpha);
    Double_t b = n/absAlpha - absAlpha;
    return a/TMath::Power(b - t, n);
  }
  if (t >= absAlpha2) {    
    Double_t c = TMath::Power(n2/absAlpha2,n2)*exp(-0.5*absAlpha2*absAlpha2);
    Double_t d = n2/absAlpha2 - absAlpha2;
    return c/TMath::Power(d + t, n2);
  }
  return 0.0;
}
    
////////////////////////////////////////////////////////////////////////////////
    
Int_t RooExtCBShape::getAnalyticalIntegral(RooArgSet& allVars, RooArgSet& analVars, const char* /*rangeName*/) const
{
  if( matchArgs(allVars,analVars,m) )
    return 1 ;
    
  return 0;
}
    
////////////////////////////////////////////////////////////////////////////////
    
Double_t RooExtCBShape::analyticalIntegral(Int_t code, const char* rangeName) const
{
  static const double sqrtPiOver2 = 1.2533141373;
  static const double sqrt2 = 1.4142135624;
    
  R__ASSERT(code==1);
  double result = 0.0;
  bool useLog = false;
  bool useLog2 = false;
    
  if( fabs(n-1.0) < 1.0e-05 ) useLog = true;
  if( fabs(n2-1.0) < 1.0e-05 ) useLog2 = true;
    
  double sig = fabs((Double_t)sigma);
    
  double tmin = (m.min(rangeName)-m0)/sig;
  double tmax = (m.max(rangeName)-m0)/sig;
    
  if(alpha < 0) {
    double tmp = tmin;
    tmin = -tmax;
    tmax = -tmp;
  }
   
  double absAlpha = fabs((Double_t)alpha);
  double absAlpha2 = fabs((Double_t)alpha2);
  
  if( tmin >= -absAlpha && tmax < absAlpha2) {
    result += sig*sqrtPiOver2*(   ApproxErf(tmax/sqrt2)
                                  - ApproxErf(tmin/sqrt2) );
  }
  else if( tmax < -absAlpha ) {
    double a = TMath::Power(n/absAlpha,n)*exp(-0.5*absAlpha*absAlpha);
    double b = n/absAlpha - absAlpha;
    
    if(useLog) {
      result += a*sig*( log(b-tmin) - log(b-tmax) );
    }
    else {
      result += a*sig/(1.0-n)*(   1.0/(TMath::Power(b-tmin,n-1.0))
                                  - 1.0/(TMath::Power(b-tmax,n-1.0)) );
    }
  }
  else if( tmin >= absAlpha2 ) {
    double c = TMath::Power(n2/absAlpha2,n2)*exp(-0.5*absAlpha2*absAlpha2);
    double d = n2/absAlpha2 - absAlpha2;
    
    if(useLog2) {
      result += c*sig*( log(d+tmax) - log(d+tmin) );
    }
    else {
      result += c*sig/(1.0-n2)*(   1.0/(TMath::Power(d+tmax,n2-1.0))
                                  - 1.0/(TMath::Power(d+tmin,n2-1.0)) );
    }
  }
  else if( tmax < absAlpha2 ) {
    double a = TMath::Power(n/absAlpha,n)*exp(-0.5*absAlpha*absAlpha);
    double b = n/absAlpha - absAlpha;
    
    double term1 = 0.0;
    if(useLog) {
      term1 = a*sig*(  log(b-tmin) - log(n/absAlpha));
    }
    else {
      term1 = a*sig/(1.0-n)*(   1.0/(TMath::Power(b-tmin,n-1.0))
                                - 1.0/(TMath::Power(n/absAlpha,n-1.0)) );
    }
    
    double term2 = sig*sqrtPiOver2*(   ApproxErf(tmax/sqrt2)
                                       - ApproxErf(-absAlpha/sqrt2) );
    
    result += term1 + term2;
  }
  else if( tmin >= -absAlpha ) {
    double c = TMath::Power(n2/absAlpha2,n2)*exp(-0.5*absAlpha2*absAlpha2);
    double d = n2/absAlpha2 - absAlpha2;
    
    double term1 = sig*sqrtPiOver2*(   ApproxErf(absAlpha2/sqrt2)
                                       - ApproxErf(tmin/sqrt2) );

    double term2 = 0.0;
    if(useLog2) {
      term2 = c*sig*( log(d+tmax) - log(n2/absAlpha2) );
    }
    else {
      term2 = c*sig/(1.0-n2)*(   1.0/(TMath::Power(d+tmax,n2-1.0))
                                  - 1.0/(TMath::Power(n2/absAlpha2,n2-1.0)) );
    }
    
    result += term1 + term2;
  }
  else {
    double a = TMath::Power(n/absAlpha,n)*exp(-0.5*absAlpha*absAlpha);
    double b = n/absAlpha - absAlpha;
    double c = TMath::Power(n2/absAlpha2,n2)*exp(-0.5*absAlpha2*absAlpha2);
    double d = n2/absAlpha2 - absAlpha2;
    
    double term1 = 0.0;
    if(useLog) {
      term1 = a*sig*(  log(b-tmin) - log(n/absAlpha));
    }
    else {
      term1 = a*sig/(1.0-n)*(   1.0/(TMath::Power(b-tmin,n-1.0))
                                - 1.0/(TMath::Power(n/absAlpha,n-1.0)) );
    }
    
    double term2 = sig*sqrtPiOver2*(   ApproxErf(absAlpha2/sqrt2)
                                       - ApproxErf(-absAlpha/sqrt2) );

    double term3 = 0.0;
    if(useLog2) {
      term3 = c*sig*( log(d+tmax) - log(n2/absAlpha2) );
    }
    else {
      term3 = c*sig/(1.0-n2)*(   1.0/(TMath::Power(d+tmax,n2-1.0))
                                  - 1.0/(TMath::Power(n2/absAlpha2,n2-1.0)) );
    }
    
    result += term1 + term2 + term3;
  }
    
  return result;
}
    
////////////////////////////////////////////////////////////////////////////////
/// Advertise that we know the maximum of self for given (m0,alpha,n,sigma)
    
Int_t RooExtCBShape::getMaxVal(const RooArgSet& vars) const
{
  RooArgSet dummy ;
    
  if (matchArgs(vars,dummy,m)) {
    return 1 ;
  }
  return 0 ;
}
    
////////////////////////////////////////////////////////////////////////////////
    
Double_t RooExtCBShape::maxVal(Int_t code) const
{
  R__ASSERT(code==1) ;
    
  // The maximum value for given (m0,alpha,n,alpha2,n2,sigma)
  return 1.0 ;
}
